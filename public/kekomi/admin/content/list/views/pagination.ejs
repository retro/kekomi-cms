<% if(state.attr("pages")){ %>
	<div class="pagination">
		<ul>
			<% for(var i = 1; i <= state.attr('pages'); i++){ %>
				<li class="<%= state.attr('page') === i ? "active" : ""%>"><a href="<%= can.route.url({state: {page: i}}, true) %>"><%= i %></a></li>
			<% } %>
		</ul>
	</div>
<% } %>