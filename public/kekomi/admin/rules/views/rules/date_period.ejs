<div class="rule-wrap form-inline clearfix date-period" data-rule="date_period">
	<div class="error"><i class="icon-warning-sign"></i></div>
	<select class="date-fields">
		<option value="">Select Field</option>
		<% for(var field in fields){ %>
			<% if(fields.hasOwnProperty(field) && fields[field] === "date"){ %>
				<option value="<%= field %>"><%= field %></option>
			<% } %>
		<% } %>
	</select>
	<label>
		<span>has a date in range</span>
		<input type="text" class="input-small range" name="start[]" <%= (el)-> el.datepicker(); el.datepicker('setDate', "+0") %> />
		<input type="text" class="input-small right-input range" name="end[]" <%= (el)-> el.datepicker(); el.datepicker('setDate', "+0") %> />
	</label>
	<a href="javascript://" class="btn btn-small remove-rule"><i class="icon-minus"></i></a>
</div>